[project]
name = "unreal-simtrace-tools"
version = "0.1.0"
description = "Validation and reporting tools for Unreal SimTrace episodes"
requires-python = ">=3.13"
dependencies = [
  "matplotlib>=3.10,<4",
  "numpy>=2.2,<3",
  "pillow>=11,<13",
]

[tool.uv]
package = false

